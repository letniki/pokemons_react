import React, {FC} from 'react';

interface IProps{
    name:string
}
const PokemonComponent:FC<IProps> = ({name}) => {

    return (
        <div>
            
        </div>
    );
};

export default PokemonComponent;